import { Layout, LogoDictionary, SearchWord } from '@/components';

const Home = () => {
    return (
        <Layout>
            <LogoDictionary />
            <SearchWord />
        </Layout>
    );
};

export default Home;
